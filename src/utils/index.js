import em from './functions/em';
import rem from './functions/rem';
import random from './functions/random';
import ease from './functions/ease';
import useTheme from './functions/useTheme';

import antialias from './mixins/antialias';
import reset from './mixins/reset';
import size from './mixins/size';
import position from './mixins/position';
import noSelect from './mixins/noSelect';
import noJitter from './mixins/noJitter';

const mixer = {
  em,
  rem,
  random,
  ease,
  useTheme,

  antialias,
  reset,
  size,
  position,
  noSelect,
  noJitter,
};

export {
  mixer,

  em,
  rem,
  random,
  ease,
  useTheme,

  antialias,
  reset,
  size,
  position,
  noSelect,
  noJitter,
};
